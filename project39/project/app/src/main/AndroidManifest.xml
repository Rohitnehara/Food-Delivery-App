<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity
            android:name=".Activity.afterCheckout"
            android:exported="false" />
        <activity
            android:name=".Activity.startingLoading"
            android:exported="false" />
        <activity
            android:name=".Activity.loadingActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.splash_activity"
            android:exported="false" />
        <activity
            android:name=".Activity.EntireMenu"
            android:exported="false" />
        <activity
            android:name=".Activity.settings"
            android:exported="false" />
        <activity android:name=".Activity.CartListActivity" />
        <activity android:name=".Activity.ShowDetailActivity" />
        <activity android:name=".Activity.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".Activity.FirebaseMessagingService"
            android:permission="TODO"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".Activity.MainActivity" />
    </application>

</manifest>